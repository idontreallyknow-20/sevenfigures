import { formMetadata } from '@/lib/noindex'

export const metadata = formMetadata('Close position')

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
